<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <%= csrf_meta_tags %>




     <link rel="stylesheet" href="/css/normalize.css">
  <!--
    application.css is where you put your styles
  -->
  <link rel="stylesheet" href="/css/application.css">

  <script src="/js/jquery.js"></script>
  <script src="/js/application.js"></script>

  </head>

  <body>
    <header role="banner" class=" pad-t-2 pad-b-4  wrap-float bg-white">
    <div class="max-w-700 center wrap-float">

      <div class="logo-container">
        <img class="logo" src="/images/logo.png" class="center block">
        <h1>Bloggin' Around</h1>
         <header role="banner" class="mar-t-5 pad-t-2 pad-b-4 pad-s-1 wrap-float bg-white">
          <div class="max-w-700 center wrap-float">
            <% if current_user %>
              <div class="pipe-separate t-light-green left">
                Signed in as <%= current_user.name %> | <%= link_to "Logout", '/logout' %>
              </div>
              <div class="pipe-separate t-light-green left">
                 <button class="button">Logout</button>
              </div>
            <%else%>
              <div class="pipe-separate t-light-green left">
                <%= link_to 'Signup', '/signup' %>
              </div>
              <div class="pipe-separate t-light-green left">
                <%= link_to 'Login', '/login' %>
              </div>
              <div class="pipe-separate t-light-green left">
            <%end%>
              </div>

           </div>

       </div>
     </div>
     <div class="pipe-separate t-light-green left">
        <div id="main_constant">
          <div id="header">
            <p><%= link_to 'All Posts', '/posts' %></p>
              <div class="Buttons">
                <button class="button"><%= link_to "New Post", new_post_path %></button>
              </div>
            </div>
            <% flash.each do |name, msg|%>
              <%= constant_tag(div: msg, class: "alert")%>
            <%end%>


  </header>

        <div class="bg-dk-green pad-t-2 pad-s-1 pad-b-8 mar-b-16 c-white">
        <div class="max-w-700 center">
        <%= yield %>
    </div>
  </div>
      </div>
      </div>
  </body>
</html>
